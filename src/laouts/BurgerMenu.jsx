import { useState } from "react"


function BurgerMenu() {
    const [isOpen, setIsOpen] = useState(false)
    const toogleMenu = () => {
        setIsOpen(!isOpen)
    }

    return (
        
    )
}

export default BurgerMenu